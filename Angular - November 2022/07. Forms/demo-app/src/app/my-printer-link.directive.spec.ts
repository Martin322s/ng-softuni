import { MyPrinterLinkDirective } from './my-printer-link.directive';

describe('MyPrinterLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new MyPrinterLinkDirective();
    expect(directive).toBeTruthy();
  });
});
